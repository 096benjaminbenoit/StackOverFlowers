{{ form_start(form, {'attr': {'class': 'w-75 mx-auto'}}) }}
    {{ form_row(form.title, {'attr': {'class': 'form-control my-2'}}) }}
    {{ form_row(form.user, {'attr': {'class': 'form-control my-2'}}) }}
    {{ form_row(form.technology, {'attr': {'class': 'my-2'}}) }}
    {{ form_row(form.content, {'attr': {'class': 'form-control my-2 py-5'}}) }}
    {# {{ form_row(form.postdate, {'attr': {'class': 'form-control'}}) }} #}
    <div class="mt-4">
        <a class="btn btn-dark" href="{{ path('app_home') }}"><i class="fa-solid fa-arrow-left pe-3" style="color: #ffffff;"></i>Back to home</a>
        <button class="btn btn-dark">{{ button_label|default('Ask') }}</button>
    </div>
{{ form_end(form) }}
